###############################################
# Qualitative Canvas — Example Environment File
# Copy to `.env` and adjust values.
###############################################

# Frontend (local dev)
FRONTEND_PORT=3000
VITE_API_URL=http://localhost:5002

# Backend (local dev)
PORT=5002
# CORS: set to your frontend origin to restrict cross-origin requests
FRONTEND_ORIGIN=http://localhost:3000

# Database selection
# DB_ENV=dev|prod  (defaults: dev locally; prod when NODE_ENV=production)
# The backend prefers DATABASE_URL_DEV/PROD, then PG*_{DEV|PROD}, then falls back to DATABASE_URL or PG*.

# Option A: Single URL for all (fallback if DEV/PROD not provided)
# DATABASE_URL=postgres://user:password@localhost:5432/qda

# Option B: Dev connection (local default)
PGHOST_DEV=localhost
PGPORT_DEV=5432
PGUSER_DEV=qc_dev
PGPASSWORD_DEV=dev_password_change_me
PGDATABASE_DEV=qda_dev

# Option C: Prod connection (used in Docker/production)
# DATABASE_URL_PROD=postgres://qc_prod:prod_password_change_me@db:5432/qda
# Or discrete:
# PGHOST_PROD=db
# PGPORT_PROD=5432
# PGUSER_PROD=qc_prod
# PGPASSWORD_PROD=prod_password_change_me
# PGDATABASE_PROD=qda

# SSL (enable if your provider requires it)
# DATABASE_SSL=true
# PGSSLMODE=require

# Optional pool tuning
# PGPOOL_MAX=10
