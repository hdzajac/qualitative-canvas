###############################################
# Qualitative Canvas — Example Environment File
# Copy to `.env` and adjust values.
###############################################

# Local frontend/backed on host
FRONTEND_PORT=3000
PORT=5002
VITE_API_URL=http://localhost:5002
FRONTEND_ORIGIN=http://localhost:3000

# Local database (Postgres runs in compose as service 'db-dev')
# Use literal connection string; do not rely on nested var expansion
DATABASE_URL=postgres://qc_dev:dev_password_change_me@localhost:5432/qda_dev

# Worker (transcription) points to local backend
SIMULATE_WHISPER=1
WHISPER_MODEL=small
WHISPER_DEVICE=cpu
WHISPER_COMPUTE_TYPE=int8
WORKER_BASE_URL=http://host.docker.internal:5002/api

# db-dev container initialization
DB_DEV_USER=qc_dev
DB_DEV_PASSWORD=dev_password_change_me
DB_DEV_DBNAME=qda_dev
DB_DEV_HOST_PORT=5432

# Full-stack Compose (optional)
# When running the full Docker profile (frontend+backend in containers), choose ONE of:
# 1) Set DOCKER_DATABASE_URL explicitly (simple, single var)
# 2) Omit DOCKER_DATABASE_URL and rely on fallback built from DB_DEV_* vars (Compose requires DB_DEV_PASSWORD)

BACKEND_HOST_PORT=5001

# Option A: explicit connection string for backend container
# DOCKER_DATABASE_URL=postgres://qc_dev:dev_password_change_me@db-dev:5432/qda_dev

# Option B: rely on fallback from DB_DEV_* (must set DB_DEV_PASSWORD)
# DB_DEV_USER=qc_dev
# DB_DEV_PASSWORD=dev_password_change_me
# DB_DEV_DBNAME=qda_dev

# Worker target when running full stack only (no local backend). Uncomment to switch:
# WORKER_BASE_URL=http://backend:5000/api

# SSL (enable if your provider requires it for remote DBs)
# DATABASE_SSL=true
# PGSSLMODE=require
